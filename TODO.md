- Проработать расположение элементов на экране
- Перепроверить mock-данные и сделать их более правдоподобными (провести исследование в perplexity о том, какие сейчас данные/benchmark по показателям)
- **useSSE**: заменить передачу bearer-токена через query-параметр на server-issued short-lived SSE ticket (токен утекает в логи и browser history)
- **useSSE**: добавить runtime-валидацию входящих SSE-событий (сейчас `JSON.parse(data) as T` без проверки схемы)
- **useSSE**: вынести `maxRetries` в параметры хука (сейчас захардкожено = 5)
- **useSSE**: добавить поддержку named events (сейчас обрабатывается только `onmessage`)
- Сделать описание каждой метрики, для чего она нужна и почему попала на Dashboard и добавить эти описания как всплывающую информацию при наведении на иконку информации
- Валидировать полученное демо относительно систем дизайна
- Подготовить README проекта
- Задеплоить демо на AWS, чтобы можно было вставить ссылку
- Перерисовать схемы из system design на excalidraw и вставить так чтобы правильно отображалось в github